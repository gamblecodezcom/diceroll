/**
 * Gamblecodez bots-hub.js — NO CHANGES REQUIRED for Dice Roll Giveaway.
 *
 * Your hub already:
 *   - POST /webhook → bot.handleUpdate(payload)
 *   - KVM1_MODE=1 → no bot.launch() / no long-polling
 *   - TELEGRAM_WEBHOOK_URL + TELEGRAM_BOT_TOKEN for setWebhook
 *
 * Register all giveaway commands in bot.js only (see bot.merge.example.js + README-INTEGRATION.md).
 */
